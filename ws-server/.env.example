PORT=8080

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=null
REDIS_PREFIX=broadcast_redis_

# Shared secret with Laravel — must match WS_SECRET in Laravel .env
WS_SECRET=change-me-strong-random-string

# Comma-separated allowed WebSocket origins
ALLOWED_ORIGINS=http://localhost:8000
